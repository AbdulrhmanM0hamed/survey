# 🚀 دليل البدء السريع

## خطوات التشغيل

### 1. تثبيت التبعيات
```bash
flutter pub get
```

### 2. توليد ملفات Hive (مطلوب مرة واحدة فقط)
```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

### 3. تشغيل التطبيق
```bash
flutter run
```

## 📱 اختبار التطبيق

بعد تشغيل التطبيق:

1. **الشاشة الرئيسية** - ستظهر قائمة الاستبيانات المتاحة
2. **اضغط على أي استبيان** للبدء في الإجابة
3. **جرب الوضع بدون اتصال** - أغلق الإنترنت وستظل البيانات متاحة

## 🔧 تشخيص المشاكل

### المشكلة: خطأ في build_runner
```bash
# حذف الملفات المولدة ثم إعادة التوليد
flutter clean
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs
```

### المشكلة: خطأ في الاتصال بالـ API
- تأكد من أن الـ API متاح على: `http://45.94.209.137:8080/api`
- جرب فتح الرابط في المتصفح للتحقق

### المشكلة: التطبيق لا يعمل بدون إنترنت
- تأكد من فتح التطبيق مرة واحدة مع الإنترنت أولاً لتحميل البيانات
- بعدها سيعمل التطبيق بدون إنترنت

## 📊 بنية المشروع

```
المشروع منظم بمعمارية MVVM:

core/          → الأساسيات (network, storage, errors)
data/          → النماذج والمستودعات
domain/        → واجهات الـ repositories
presentation/  → الشاشات والـ ViewModels
```

## ✅ التحقق من الإعداد

```bash
# التحقق من صحة الكود
flutter analyze

# تشغيل الاختبارات
flutter test
```

## 🎯 نقاط مهمة

- ✅ جميع الإجابات تُحفظ تلقائياً
- ✅ التطبيق يعمل بدون إنترنت بعد أول تحميل
- ✅ نظام الشروط يعمل ديناميكياً
- ✅ واجهة كاملة بالعربية

---

**للمساعدة:** راجع ملف [README.md](README.md) للتفاصيل الكاملة
